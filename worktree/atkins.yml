#!/usr/bin/env atkins
tasks:
  default:
    desc: "Capture workspace screenshots"
    steps:
      - goimports -w .
      - go fmt ./...
      - gotestsum ./... -- -count 1
      - go install .
      - defer: rm -f examples/*.txt
      - worktree -puml | plantuml -tsvg -pipe > examples/workspace.svg
      - worktree -d2 | d2 --layout elk - examples/workspace-d2.svg
