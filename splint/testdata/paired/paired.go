// Package paired has a file with a test beside it and a file without one,
// which is what the pairing check counts.
package paired

// Tested is here so paired.go has something to test.
func Tested() int {
	return 1
}
