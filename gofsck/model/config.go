package model

// Config holds shared configuration for all analyzers.
type Config struct {
	// Future fields can be added here as needed
	// For now, this serves as the common interface for all analyzers
}
